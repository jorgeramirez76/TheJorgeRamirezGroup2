# Mail credentials for the daily SEO report — copy to `mail.env` (git-ignored).
#
# EASIEST: a Gmail App Password (60 seconds, needs 2-Step Verification on):
#   1. Go to  https://myaccount.google.com/apppasswords
#   2. Create one named "SEO Doctor" — Google shows a 16-character password.
#   3. Paste it below (remove the spaces), then delete this comment block.
#
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=jorgeramirez76@gmail.com
SMTP_FROM=jorgeramirez76@gmail.com
SMTP_PASS=your16charapppassword
